import React from 'react'
import { CardHeader } from '@material-ui/core';

export const Header = () => {
    return(
        <div>
            <CardHeader >
                Corona virus Cases

            </CardHeader>
        </div>
    )
}